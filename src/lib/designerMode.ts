// Designer-storyboard mode flag.
// When true, the StoryWizard skips the standard authoring pipeline entirely
// (no warm-up, no persona/problem/solution/standard-storyboard generation,
// no speculative or final renders) and instead drives a per-scene variant
// picker against the static manifest in src/data/designerStoryboards.ts.
export const ENABLE_DESIGNER_STORYBOARD_MODE = true;
